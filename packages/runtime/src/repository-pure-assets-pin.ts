/** Build output replaces the compiled copy only. Source execution must use the
 * explicit asynchronous constructor; an unbuilt installation fails closed. */
export const BUILT_REPOSITORY_PURE_PRODUCER_DESCRIPTOR_DIGEST: string | null = null;
