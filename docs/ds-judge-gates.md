# DS Judge: implementation and local verification

Checkpoint: 2026-09-05, production application build. The application runs locally and a standalone Windows package has measured installation and replay evidence. Live-model investigation reliability remains limited: the latest fixed four-attempt cohort qualifies **1/4**. This is an internal demonstration checkpoint, not a dependable paid-release claim.

The [current 950-file freeze](../artifacts/provider-connections-installed-freeze.json) includes visual Connections setup. The [workspace build](../artifacts/provider-connections-build.log) and [workspace typecheck](../artifacts/provider-connections-workspace-typecheck.log) pass. The [full test run](../artifacts/provider-connections-source-validation.json) passes **1,319 tests with 14 skips and no failures**; its installed package and daemon dependencies remained unchanged. The previous [929-file checkpoint](../artifacts/production-launch-final-freeze.json) and earlier overlapping test scopes remain historical evidence.

Connections discovers local models, configures compatible cloud APIs through origin-bound credential references, exports MCP client configuration, and tests registered MCP servers. The [nine-check isolated browser proof](../artifacts/provider-connections-browser-source-v2/report.json), [four-check save/restart proof](../artifacts/provider-connections-source-fixture-v2/activation-proof.json), and [eight-check production browser proof](../artifacts/provider-connections-production-browser/report.json) pass. The live Ollama service listed 17 model IDs. The Windows launcher honors saved profiles, including model-family and tool-format choices. Native agent configuration remains separate. These setup checks do not run cloud inference or qualify a model; the MCP handshake fixture is synthetic. See [connection setup](provider-connections.md).

| Area | Implemented behavior and measured scope |
| --- | --- |
| Investigation and Reflex | Captured execution feedback, committed-parent inspection, executable revision scheduling, honest refusal and the two-loop cap are implemented. The [current model cohort](../artifacts/live-model-qualification-production-launch-1788608737491/summary.json) qualifies one clean Qwen attempt. The other three attempts are INVALID; all four signed Records independently replay. There were 21 logical calls, six failures, 59 started provider rounds, eight distinct Docker invocations and nine sandbox evidence events. Three feedback invocations produced no admitted revision. This does not demonstrate model repair or a causal prompt improvement. |
| Original-source judgment | Explicit Bone v2 admits independently reconstructed assertion certificates for a [closed finite JavaScript subset](repository-evaluator-authority.md). The [three retained original-source proofs](../artifacts/original-subject-installed-lifecycle-1788604306738/independent-verification.json) reach ACCEPT, REJECT and UNPROVEN. Unsupported repositories and generic audit missions remain outside this oracle. These investigators are deterministic, not language models. |
| Revision execution | A [signed compiled comparative-repair lifecycle](../artifacts/comparative-repair-lifecycle-1788606210000/independent-verification.json) preserves two failed parents, executes a repaired child and reaches ACCEPT with six physical Docker invocations. This is deterministic scheduler/execution evidence under its named checkpoint, separate from current model quality. |
| Additive Juggler | Five quantities have bounded scheduler effects and no verdict authority. The [current calibration](../artifacts/metabolic-calibration-production-launch-1788608673726/independent-verification.json) verifies 64 paired seeds, 320 trusted-source executions and 4,788 artifacts. [Checked local installation/startup loading](../artifacts/production-launch-calibration-startup.json) binds 4,802 snapshot entries and permits all five kinds up to dose two for the current method. This is finite scheduler calibration, not live-model efficacy. |
| Production UI and onboarding | Normal startup uses compiled production assets and a filtered child environment. [Custom-port browser verification](../artifacts/chamber-production-custom-port-1788608780000/report.json) checks four routes, 22 assets, hydration, exact API routing and 320/736/1024 widths. [Cold Airlock fixes](../artifacts/airlock-launch-source-1788609498000/report.json) open the editor before service shutdown and preserve Windows query strings. Fresh init selects Bone v2; existing policies remain unchanged. See [new projects](new-projects.md). |
| Distribution | The [standalone calibrated package](../artifacts/app-release-provider-connections-1788628007107.build.json) includes the compiled application and production dependencies, with optional verified calibration. The [outside-checkout proof](../artifacts/app-release-provider-connections-proof-1788628007107/report.json) passes twelve checks, including Connections save/restart activation, fresh init, explicit calibration installation, refusal to replace an existing installation, production HTTP, a signed Record, offline replay and byte-tamper refusal. Node 24, models and OCI engines remain separate. This is Windows x64 evidence on this host, not a clean-machine or cross-platform certification. |
| Record and historical replay | Exact Seal/Record/terminal signatures, ledger/artifact inventories and reason-to-evidence navigation are implemented. A [portable matching verifier](replay-release.md) has eight isolated checks. [Current live browser QA](../artifacts/ds-judge-live-browser-production/report.json) passes 14 read-only checks on the restored app and a named authenticated Record. Neither valid replay nor a rendered Record establishes useful model output. |
| Genome, memory and self-judgment | Signed governance, paired comparisons, quarantine, first-wave amnesia, bounded late influence, contamination closure and opt-in self-watching remain implemented. Their physical proofs retain their original scope in the [historical checkpoint](ds-judge-gates-original-subject-checkpoint.md). No real Genome promotion or broad live-model benefit is claimed. |
| Transports and integrations | CLI/TUI, TS/Python SDKs, HTTP/SSE, multipart, JSONL, drop bundles, MCP, inbound A2A and exact-SHA advisory integration remain implemented. The historical 14-Case corpus records constitutional coverage. Credentialed native-agent inference, live Podman and offered remote publication/attestation paths still need direct qualification. |

The restored local app uses the compiled production Chamber. [Startup](../artifacts/ds-judge-live-startup-production.json) and [doctor](../artifacts/ds-judge-live-doctor-production.json) confirm two available local models and Docker; this is infrastructure readiness. Named self-audit case case_30560ed49815f178 is retained through [ordinary export](../artifacts/ds-judge-live-self-production/export.json) and [independent CLI replay](../artifacts/ds-judge-live-self-replay-production.json). Its provider failure is not evidence of a repository defect or a claim about subsequent watcher Cases.

The latest measured model cohort froze 929 files before the later CLI and Connections changes. The current 950-file application has different presentation, configuration and SDK bytes; investigation method 5974978c18f2862c3be54c1328821880c2144a0b7cb25538c3c95b319eda820b, metabolic method 9e800eb68743640e5e6ad82f67d1ad796c9c00b9dc2eeac82a1631020c5c6a20, and original-source producer identity remain unchanged. Previous 0/4 cohorts, failed harness attempts and all older proofs remain separate evidence.

The previous standalone archive also passes [five cold Airlock opener checks](../artifacts/app-release-production-cold-proof-1788609800628/report.json) and [three historical original-source replay checks](../artifacts/app-release-historical-replay-1788609961005/report.json). Its [strict browser report](../artifacts/standalone-production-browser-1788610100002/report.json) remains failed for an automatic favicon.ico 404; [15 functional checks](../artifacts/standalone-production-browser-1788610100002/functional-summary.json) pass. The current build explicitly links the existing SVG icon, and its production browser proof has no HTTP or browser errors. That fix does not rescore the historical failure.

Couch/Horizon design is maintained by its separate owner. These infrastructure and route checks do not establish that experience's usability or market value. The [release-readiness checklist](release-readiness.md) distinguishes internal demonstration, assisted pilot and paid release. [Application packaging](app-release.md), [providers](agent-providers.md), [investigation](autonomous-investigation.md) and [metabolism](metabolism.md) describe the supported boundaries.
